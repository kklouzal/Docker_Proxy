[pytest]
markers =
    live: exercises a running container stack through real HTTP endpoints
    legacy_simulated: relies on in-process monkeypatches or test doubles and is a candidate for live-stack replacement