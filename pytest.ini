[pytest]
testpaths = web/tests
norecursedirs = .venv .git artifacts
markers =
    live: exercises a running container stack through real HTTP endpoints
    integration: exercises optional external services or full-stack integrations
    mysql: requires a configured MySQL test database
