name: squid-flask-proxy

services:
  admin-ui:
    extends:
      file: docker-compose.common.yml
      service: admin-ui
    build:
      context: .
      dockerfile: docker/Dockerfile.admin
      pull: true

networks:
  control:
