# Minimal freshclam config for squid-flask-proxy

# Signature DB location (persisted).
DatabaseDirectory /var/lib/squid-flask-proxy/clamav/db

# Default database mirror (required on some distros/builds)
DatabaseMirror database.clamav.net

# Update cadence (times/day)
Checks 12

# DNS: freshclam uses /etc/resolv.conf by default, which Docker populates from
# the container's dns: setting in docker-compose.yml (or --dns on the CLI).
# Do NOT hardcode DNSNameServer here — it prevents the deployment environment
# from controlling DNS resolution for the container.

# Use a predictable user when present
DatabaseOwner clamav
